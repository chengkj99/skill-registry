# 发布到 npmjs 时使用环境变量，避免把 token 写进仓库
# 在 npm 网站创建 Granular Access Token（勾选 Bypass 2FA）后：
#   export NPM_TOKEN='npm_...'
#   npm publish
registry=https://registry.npmjs.org/
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
